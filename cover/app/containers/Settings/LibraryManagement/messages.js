/*
 * LibraryManagement Messages
 *
 * This contains all the text for the SettingsLibraryManagement container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LibraryManagement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LibraryManagement container!',
  },
});

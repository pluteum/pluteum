/*
 * BookUpload Messages
 *
 * This contains all the text for the BookUpload container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BookUpload';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BookUpload container!',
  },
});
